import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { Toaster } from "@/components/ui/toaster"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "NVHealth Labs - Online Diagnostic Lab Booking",
  description:
    "Book lab tests online with trusted diagnostic centers. Secure, fast, and convenient healthcare testing.",
  keywords: "lab tests, diagnostic center, blood tests, health checkup, online booking",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster />
      </body>
    </html>
  )
}
